<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Editando: <%= book.title %></title>
        <link rel="stylesheet" href="/css/styles.css" />
    </head>
    <body>
        <h1 class="title">
            <span class="icon">✏️</span> Editando: <%= book.title %>
        </h1>
        <form action="/library/<%= book.id %>?_method=PUT" method="POST">
            <div class="input-group">
                <label><span>📖</span> Título do Livro:</label>
                <input
                    type="text"
                    name="title"
                    value="<%= book.title %>"
                    placeholder="Título:"
                    required
                />
            </div>

            <div class="input-group">
                <label><span>💰</span> Preço:</label>
                <input
                    type="number"
                    step="0.01"
                    name="price"
                    value="<%= book.price %>"
                    placeholder="$00.00"
                    required
                />
            </div>

            <div class="input-group">
                <label><span>🧾</span> Descrição:</label>
                <textarea name="description" placeholder="Descrição:" required>
                <%= book.description %>
            </textarea
                >
            </div>

            <button class="add-button" type="submit">Salvar</button>
        </form>
        <a class="back-button" href="/library"
            ><span class="icon"><-</span> Voltar</a
        >
    </body>
</html>
